---
description: Update flake inputs and refresh lockfile
agent: nixos-planner
# model: default
---
Update the flake inputs (nixpkgs, home-manager, modules) and propose a safe `nix flake update` plan. Provide diff suggestions and notes on compatibility risk.
